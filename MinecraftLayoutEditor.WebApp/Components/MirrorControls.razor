<div class="settings-group">
    <span>Mirror Settings</span>
    <!-- Mirror -->
    <div class="form-check">
        <input type="checkbox" id="mirrorEnabled"
            checked="@IsMirrorEnabled"
               @onchange="@((e) => OnSymmetryAxisChanged((bool)e.Value!))" />
        <label for="mirrorEnabled">
            Enable mirroring
        </label>
    </div>

    <!-- MirrorAxis -->
    <div class="form-check">
        <input type="checkbox" id="isHorizontal"
               @bind="IsHorizontal" @bind:after="OnIsHorizontalChanged" disabled="@(!IsMirrorEnabled)" />
        <label for="isHorizontal">
            Horizontal mirroring
        </label>
    </div>

    <!-- RotationDeg -->
    <div class="form-group">
        <label for="rotation">Rotation (deg)</label>
        <select id="rotation" class="form-control"
                @bind="RotationDeg" @bind:after="OnRotationDegChanged"
                disabled="@(!IsMirrorEnabled)">
            <option value="0">0°</option>
            <option value="180">180°</option>
        </select>
    </div>
</div>
